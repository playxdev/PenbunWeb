# Cloudflare Pages — static deploy config for PenbunWeb.
# `public/` is the whole deployable artifact; no Functions, no bindings.
name = "penbunweb"
compatibility_date = "2026-01-01"
pages_build_output_dir = "./public"
