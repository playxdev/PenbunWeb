{
  "name": "penbunweb",
  "version": "1.4.0-beta.1",
  "private": true,
  "description": "PenbunWeb — HTML + CSS + TypeScript front end for Penbun System (UI beta; auth and master data talk to PenbunAPI)",
  "type": "module",
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('public/assets/js',{recursive:true,force:true})\"",
    "build": "tsc -p tsconfig.json && node tools/dist.mjs",
    "gen:master": "npm run build && node tools/gen_master_pages.mjs",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "node tools/serve.mjs",
    "dev": "npm run build && npm run serve",
    "preview": "npm run build && npx wrangler pages dev dist",
    "deploy": "npm run build && npx wrangler pages deploy",
    "clean": "rm -rf public/assets/js dist",
    "test": "npm run build && node tools/test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
